/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        remotePatterns: [{
          protocol: 'https',
          hostname: 'mjeggw6uz9.ufs.sh',
        }]
      },
};

export default nextConfig;